import React from "react";
import "./Header.css";

function Header() {
  return (
    <header className="header">
      <div className="logo">Logo</div>
      <nav>
        {/* <a href="#">Home</a>
        <a href="#">Member</a>
        <button>Login</button> */}
      </nav>
    </header>
  );
}

export default Header;
